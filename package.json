{
  "name": "dialog-export",
  "version": "0.1.0",
  "private": true,
  "description": "Local-only AI web conversation Markdown exporter for Chrome and Microsoft Edge.",
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit && vite build --mode popup && vite build --mode background && vite build --mode content",
    "dev": "vite build --mode popup --watch",
    "icons": "node scripts/generate-icons.mjs",
    "check:release": "npm run icons && npm run build && node scripts/release-check.mjs",
    "clean": "powershell -NoProfile -Command \"if (Test-Path dist) { Remove-Item -Recurse -Force dist }\""
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "@types/node": "^20.14.15",
    "sharp": "^0.35.3",
    "typescript": "^5.5.4",
    "vite": "^5.4.11"
  }
}
